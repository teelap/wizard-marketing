# Used ideas — LEGACY (deprecated 2026-06-15)

**This folder is no longer used by the engine.** Jake's call (2026-06-15): `content/Ideas/` is a
**permanent library of his top concepts** — the routines *draw* from it but **never consume,
stamp, or retire** anything. No concept gets moved here anymore; every concept stays in the
active pool indefinitely and is meant to return (re-angled) over time. Usage is tracked in the
weekly calendar log, not by moving files.

Kept only as an archive in case Jake wants to manually shelve a concept. The old "retire when
shipped on both surfaces, recycle when the pool runs dry" lifecycle is gone — see
`content/Ideas/_INDEX.md` and the `weekly-evergreen` skill §8 for the current model.
